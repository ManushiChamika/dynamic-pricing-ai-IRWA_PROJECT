# Pricing agent output artifacts
app/pricing_agent_run.json
app/pricing_agent_dbrows.json

########################################################
# .gitignore - purpose and guidance
# This file prevents local, generated, and sensitive files
# from being tracked by Git. Comments explain why each
# section/pattern exists so contributors don't remove them.
########################################################

# ---------- Python ----------
# Ignore Python bytecode and compiled files generated at runtime
__pycache__/
_pycache_/
*.py[cod]
*.pyo
*.pyd

# ---------- Virtual environments ----------
# Local virtualenv folders contain installed packages and platform-
# specific binaries. These are heavy and should not be committed.
venv/
env/
.venv/
.bundle_env/

# ---------- IDE / Editor ----------
# Editor specific settings are personal or machine-specific.
.vscode/
.idea/

# ---------- OS files ----------
# OS creates thumbnail and metadata files which are irrelevant to the repo.
.DS_Store
Thumbs.db

# ---------- Databases & logs ----------
# Local DB files and logs may contain sensitive data (users, credentials).
# They also change frequently and bloat the repo.
*.sqlite
*.sqlite3
*.db
app/*.db
# market.db is ignored
data/market.db
.env
.env.*
*.log
data/events.jsonl
auth.db
app/alert.db
user_data.json
.llm_cache/

# ---------- Patches / diffs ----------
# Local patch/diff files created during development should not be tracked.
*.diff

# ---------- Archives & build artifacts ----------
# Build outputs, distributions and cache directories can be large and
# are regenerated by build tools, so we ignore them.
*.zip
*.tar.gz
dist/
build/
.cache/
.ipynb_checkpoints/

# ---------- Streamlit ----------
# Streamlit stores local secrets and runtime state; don't commit.
.streamlit/secrets.toml
.streamlit/

# ---------- Node / JS ----------
# Node modules are installed by package managers and are not tracked.
node_modules/

# ---------- Project-specific / scripts ----------
# Local dumps, prompts and scripts used for development or data
# collection should not be committed (may contain data or secrets).
project_code_dump.txt
prompts.md
scripts/collect_project_code.ps1
AGENTS.md
BRANCH_DIFFERENCES_README.md

# Specific generated text artifacts (root-scoped)
_staged_diff_full.txt
/files.txt
/all_code_files_pricing_optimizer1_default.txt
PRICING_OPTIMIZER_CAPABILITIES.md

# Newly generated documentation (auto-ignore)
SIMPLE_PRICE_OPTIMIZER_MAP.md
GOVERNANCE_AGENT_EXPLAINED_FOR_KIDS.md
FRONTEND_PRICE_OPTIMIZER_MAP.md
PRICE_OPTIMIZER_IMPROVEMENTS.md
LLM_IMPROVEMENTS_FOR_PRICE_OPTIMIZER.md

# Local working notes (root-only)
 /imasha_compare_restore_vs_backup.txt
 /summary.md
 /_staged_diff_summary.txt
 /visual_file_tree.txt
 /branch_notes_feat_data_collector_mvp.md
 /branch_notes_feat_improve_data_collector.md
 /error to the cli agent.md
 /my thoughts on the ui.md
 /my thougts on ui.md
 /branch_differences_feat_ui_activity_feed_vs_testing_sasindu.md

# ---------- Local code bundles (ignore large generated bundles) ----------
bundle_*/

# ---------- Notebooks / checkpoints ----------
.ipynb_checkpoints/

# ---------- Tracked exceptions ----------
# Keep example env files tracked so new contributors know required vars.
!.env.example

########################################################
# End of explanatory comments
########################################################

# Cleanup additions (auto)
.bundle_env/
*.pyc

# Test/runtime caches
.pytest_cache/
.hypothesis/
.mypy_cache/
.coverage
htmlcov/

# SQLite journals
*.db-shm
*.db-wal
*.db-journal

# Frontend test outputs
frontend/playwright-report/
frontend/test-results/

"catalog_analysis.txt"  
