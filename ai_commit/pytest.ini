[pytest]
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning:pydantic.*:
    ignore:.*Pydantic V1 style.*:DeprecationWarning
    ignore:.*min_items is deprecated.*:DeprecationWarning
addopts = -q
# Exclude flaky or environment-dependent tests by default
norecursedirs = scripts
